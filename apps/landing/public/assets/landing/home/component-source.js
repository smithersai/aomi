document.getElementById("aomi-home-component").textContent = "\nclass Component extends DCLogic {\n  state = { played: false, bubbleOn: false, typed: 0, traceOn: false, shown: 0, running: true, collapsed: false, answerOn: false, expanded: false, signOn: false, signState: 'idle', wtab: 0 };\n  WALLETS = [\n    { provider: '', env: '', importOn: false, foot: 'Browser mode authenticates an existing EOA. No Para or Privy provider import required.' },\n    { provider: 'para', env: 'PROD', importOn: true, foot: 'Importing the Para entry point registers Para authentication and its wallet signer.' },\n    { provider: 'privy', env: 'production', importOn: true, foot: 'Importing the Privy entry point registers Privy authentication and its wallet signer.' },\n  ];\n  MSG = \"Stake 5 ETH with Lido, then use that staked position as collateral on Aave and borrow 1000 USDC against it\";\n  ITEMS = [\n    { kind: 'tool', title: 'Activate skills', chips: ['lido', 'aave', 'common_erc20'] },\n    { kind: 'tool', title: 'Check ETH balance', chips: ['Ethereum', '10 ETH', 'nonce 2751'] },\n    { kind: 'note', text: 'Wallet holds 10 ETH — enough for the 5 ETH stake. Reading the wstETH rate to size the wrap and supply legs precisely.' },\n    { kind: 'tool', title: 'Read wstETH per stETH rate', chips: ['1 stETH → 0.8059 wstETH'] },\n    { kind: 'tool', title: 'Simulate Lido stake of 5 ETH', chips: ['5 ETH → ~5 stETH', '~4.0297 wstETH'] },\n    { kind: 'note', text: 'All context checks pass — borrowing 1000 USDC against ~4.03 wstETH is well within safe LTV. Staging the full 6-step sequence.' },\n    { kind: 'tool', title: 'Stage stake → wrap → supply → borrow', chips: ['Stake 5 ETH with Lido', 'Approve stETH', 'Wrap stETH → wstETH', 'Approve wstETH', 'Supply to Aave V3', 'Borrow 1000 USDC'] },\n    { kind: 'tool', title: 'Simulate full 6-tx flow', chips: ['all 6 pass', 'gas 1.04 gwei'] },\n    { kind: 'tool', title: 'Commit batch', chips: ['0x5919…d464', 'success'] },\n  ];\n  CAPTIONS = [\n    { t: 'Internal skills with protocol-specific guardrails', s: 'Lido, Aave and ERC-20 tooling load the moment the intent needs them.' },\n    { t: 'Resolves stETH and wstETH', s: 'Aave takes wstETH, not raw stETH — the agent routes through the wrap unprompted.' },\n    { t: 'Simulate in forked fleet of chains', s: 'Every leg dry-runs against a fork of mainnet, then the whole chain runs once more end to end.' },\n    { t: '6 Transactions → 1 Bundle → Sign & Broadcast', s: 'Staked, wrapped, supplied, borrowed — atomically, with the hash to prove it.' },\n  ];\n  componentDidMount() {\n    const a = document.getElementById('om-proto-a');\n    const b = document.getElementById('om-proto-b');\n    if (a && b && !b.childNodes.length) b.innerHTML = a.innerHTML;\n    this.timers = [];\n    const target = document.getElementById('om-trace-showcase');\n    if (!target) { this.start(); return; }\n    const inView = () => {\n      const r = target.getBoundingClientRect();\n      return r.top < window.innerHeight * 0.7 && r.bottom > window.innerHeight * 0.2;\n    };\n    const check = () => {\n      if (!this.state.played && inView()) this.start();\n    };\n    this.onScroll = check;\n    window.addEventListener('scroll', check, { passive: true });\n    window.addEventListener('resize', check, { passive: true });\n    this.poller = setInterval(check, 500);\n    check();\n    if (typeof IntersectionObserver !== 'undefined') {\n      this.io = new IntersectionObserver(check, { threshold: 0.35 });\n      this.io.observe(target);\n    }\n  }\n  componentWillUnmount() {\n    (this.timers || []).forEach(clearTimeout);\n    clearInterval(this.typer);\n    clearInterval(this.poller);\n    if (this.onScroll) {\n      window.removeEventListener('scroll', this.onScroll);\n      window.removeEventListener('resize', this.onScroll);\n    }\n    if (this.io) this.io.disconnect();\n  }\n  resetRun() {\n    (this.timers || []).forEach(clearTimeout);\n    this.timers = [];\n    clearInterval(this.typer);\n    this.setState({ played: true, bubbleOn: false, typed: 0, traceOn: false, shown: 0, running: true, collapsed: false, answerOn: false, expanded: false, signOn: false, signState: 'idle' });\n  }\n  at(ms, fn) { this.timers.push(setTimeout(fn, ms / (this.props.pace ?? 1))); }\n  pinScroll() {\n    const v = document.getElementById('om-trace-viewport');\n    if (v) v.scrollTop = v.scrollHeight;\n  }\n  start() {\n    const p = this.props.pace ?? 1;\n    this.setState({ played: true, bubbleOn: true });\n    this.at(250, () => {\n      this.typer = setInterval(() => {\n        this.setState((s) => {\n          if (s.typed >= this.MSG.length) { clearInterval(this.typer); this.afterTyping(); return null; }\n          return { typed: s.typed + 1 };\n        });\n      }, 12 / p);\n    });\n  }\n  afterTyping() {\n    this.at(380, () => this.setState({ traceOn: true }));\n    const first = 900, gap = 640;\n    const pre = this.ITEMS.length - 1;\n    this.ITEMS.slice(0, pre).forEach((_, i) => {\n      this.at(first + i * gap, () => this.setState({ shown: i + 1 }, () => this.pinScroll()));\n    });\n    const signAt = first + pre * gap + 250;\n    this.at(signAt, () => this.setState({ signOn: true, signState: 'idle' }, () => this.pinScroll()));\n    this.at(signAt + 1100, () => this.setState({ signState: 'pressed' }));\n    this.at(signAt + 1300, () => this.setState({ signState: 'signed' }));\n    this.at(signAt + 1900, () => this.setState({ shown: this.ITEMS.length }, () => this.pinScroll()));\n    const end = signAt + 1900 + gap;\n    this.at(end + 150, () => this.setState({ running: false }));\n    this.at(end + 700, () => this.setState({ traceOn: false, collapsed: true }));\n    this.at(end + 1150, () => this.setState({ answerOn: true }));\n    if (this.props.loop ?? true) this.at(end + 4600, () => { if (this.state.expanded) { this.loopWaiting = true; return; } this.resetRun(); this.at(400, () => this.start()); });\n  }\n  surfaceVals() {\n    const s = this.state.surf ?? 0;\n    const out = {\n      surfIsWeb: s === 0, surfIsTg: s === 1, surfIsDc: s === 2, surfIsSl: s === 3,\n    };\n    ['Web', 'Tg', 'Dc', 'Sl'].forEach((k, i) => {\n      out[`surf${k}`] = () => this.setState({ surf: i });\n      out[`surf${k}Border`] = i === s ? '#5288c2' : '#e4e4e7';\n      out[`surf${k}Bg`] = i === s ? '#5288c2' : '#f4f4f5';\n      out[`surf${k}Color`] = i === s ? '#fff' : '#71717a';\n    });\n    return out;\n  }\n  terminalVals() {\n    const m = this.state.wmode ?? 0, t = this.state.ttab ?? 0;\n    const out = {\n      wmIsUi: m === 0, wmIsTerm: m === 1,\n      wmUi: () => this.setState({ wmode: 0 }), wmTerm: () => this.setState({ wmode: 1 }),\n      ttab0: () => this.setState({ ttab: 0 }), ttab1: () => this.setState({ ttab: 1 }),\n      tIsCli: t === 0, tIsMcp: t === 1,\n      tCaption: t === 0 ? 'Bring a key, a wallet signature, or just a Google login. All three work.' : 'OAuth binds the session to your Aomi account. Wallets are account-scoped, so there is no connect step and no keys touch the agent.',\n    };\n    [['wmUi', m === 0], ['wmTerm', m === 1]].forEach(([k, on]) => {\n      out[k + 'Bg'] = on ? '#5288c2' : 'transparent';\n      out[k + 'Color'] = on ? '#fff' : '#71717a';\n    });\n    [0, 1].forEach((i) => {\n      out[`t${i}Color`] = i === t ? '#09090b' : '#71717a';\n      out[`t${i}Weight`] = i === t ? 600 : 500;\n      out[`t${i}Border`] = i === t ? '#09090b' : 'transparent';\n    });\n    return out;\n  }\n  faqVals() {\n    const data = [\n      {\n            \"q\": \"How does Aomi work with different wallets and private-key infrastructure?\",\n            \"a\": \"Aomi separates transaction execution from key custody. With browser or embedded wallets, Aomi sends the prepared transaction to the wallet for user approval. Key-holding clients can sign automatically at the edge, while custodial providers can execute through an explicit delegated grant. The model never receives private-key material or chooses the signing path; Aomi resolves the configured wallet, authorization mode, and provider independently at commit time.\"\n      },\n      {\n            \"q\": \"Where is transaction policy enforced, and can the model bypass it?\",\n            \"a\": \"Policy is enforced by Aomi’s execution layer, outside the model. Applications can constrain permitted chains, contracts, function selectors, token-approval spenders, Solana programs, instruction types, and transaction values. Policies compose by intersection: every applicable guard must approve an action, and none can widen another’s authority. Signing permission is checked again at commit time, so constructing a valid transaction does not grant permission to execute it.\"\n      },\n      {\n            \"q\": \"Where does Aomi sit relative to wallet providers and agent frameworks?\",\n            \"a\": \"Wallet providers manage accounts, keys, and signatures. Agent frameworks reason about intent and call tools. Aomi provides the transaction layer between them: it converts intent into chain-native actions, applies application policy, simulates the resulting transactions, coordinates authorization, and routes execution through the selected signer. Wallet providers and agent platforms can therefore use Aomi without being replaced by it.\"\n      },\n      {\n            \"q\": \"How does Aomi preserve transaction integrity from intent through execution?\",\n            \"a\": \"Transactions are staged into authoritative runtime state and assigned stable identifiers. Downstream simulation, approval, and commit steps resolve those stored transactions instead of asking the model to reconstruct calldata. Aomi validates wallet and network context, applies execution guards, simulates the proposed batch, and sends the resolved payload to the signer. Returned signatures and transaction hashes are correlated with the originating request so the workflow can continue from the actual execution result.\"\n      },\n      {\n            \"q\": \"How does account abstraction work within Aomi’s execution model?\",\n            \"a\": \"Account abstraction is an application-level execution policy, not a choice made by the model. When enabled, Aomi can route eligible actions through a provisioned smart account for batching, scoped authorization, and sponsored network fees. The connected wallet remains the owner and authorization identity, while the smart account becomes the effective execution account. If the required sponsored route cannot be satisfied, Aomi stops instead of silently falling back to a different account or user-funded transaction.\"\n      },\n      {\n            \"q\": \"How does Aomi integrate with an existing product stack?\",\n            \"a\": \"Integrators keep their existing application, authentication system, wallet provider, and signing experience. They supply authenticated user and wallet context through Aomi’s Widget or REST APIs; Aomi handles intent processing, transaction construction, policy enforcement, simulation, signing handoff, and workflow continuation. This allows teams to add agentic execution without rebuilding wallet infrastructure or exposing private keys to the model.\"\n      }\n];\n    const open = this.state.faqIdx ?? 0;\n    return {\n      faqs: data.map((f, i) => ({\n        ...f,\n        open: open === i,\n        mark: open === i ? \"−\" : \"+\",\n        toggle: () => this.setState({ faqIdx: open === i ? -1 : i }),\n      })),\n    };\n  }\n  installVals() {\n    const tools = [\n      { label: 'Skills', command: 'npx skills add aomi-labs/skills', logo: (window.__resources && window.__resources.logo_cursor) || './logos/cursor.svg' },\n      { label: 'CLI', command: 'npm install -g @aomi-labs/client', logo: (window.__resources && window.__resources.logo_cli) || './logos/cli.svg' },\n      { label: 'MCP', command: 'npx skills add aomi-labs/skills', logo: (window.__resources && window.__resources.logo_mcp) || './logos/mcp.svg' },\n      { label: 'API', command: 'curl https://api.aomi.dev/v1/agent/chat \\\\\\n  -H \"Authorization: Bearer $AOMI_TOKEN\" \\\\\\n  -d \\'{ \"message\": \"Swap 0.5 ETH to USDC on Base\" }\\'', logo: (window.__resources && window.__resources.logo_api) || './logos/api.svg' },\n    ];\n    const i = this.state.instTool ?? 0, sel = tools[i];\n    return {\n      instCommand: sel.command, instLabel: sel.label,\n      instLogoImg: React.createElement('img', { src: sel.logo, style: { width: 15, height: 15, flex: 'none' } }),\n      instLogoImgLight: React.createElement('img', { src: sel.logo, style: { width: 15, height: 15, flex: 'none', filter: 'brightness(0) invert(1)' } }),\n      heroFlip: (e) => { if (e && e.preventDefault) e.preventDefault(); this.setState(s => ({ heroFlipped: !s.heroFlipped, instOpen: false })); },\n      heroFlipT: this.state.heroFlipped ? 'rotateY(180deg)' : 'rotateY(0deg)',\n      heroFrontPE: this.state.heroFlipped ? 'none' : 'auto',\n      heroBackPE: this.state.heroFlipped ? 'auto' : 'none',\n      instAlign: sel.command.includes('\\n') ? 'left' : 'center',\n      instRowMax: sel.command.includes('\\n') ? '760px' : '560px',\n      instOpen: !!this.state.instOpen, instCopied: !!this.state.instCopied, instNotCopied: !this.state.instCopied,\n      instToggle: () => this.setState(s => ({ instOpen: !s.instOpen })),\n      instCopy: () => {\n        try { navigator.clipboard.writeText(sel.command); } catch (e) {}\n        this.setState({ instCopied: true });\n        clearTimeout(this._instCt);\n        this._instCt = setTimeout(() => this.setState({ instCopied: false }), 1400);\n      },\n      instTools: tools.map((t, j) => ({ ...t, on: j === i, bg: j === i ? '#f4f4f5' : 'transparent',\n        logoImg14: React.createElement('img', { src: t.logo, style: { width: 14, height: 14, flex: 'none' } }),\n        logoImg16: React.createElement('img', { src: t.logo, style: { width: 16, height: 16 } }),\n        pick: () => this.setState({ instTool: j, instOpen: false, instCopied: false }) })),\n    };\n  }\n  renderVals() {\n    const s = this.state;\n    const typingDone = s.typed >= this.MSG.length;\n    const rows = this.ITEMS.slice(0, s.shown).map((item, i) => {\n      const isTool = item.kind === 'tool';\n      const active = s.running && i === s.shown - 1;\n      const chips = (item.chips || []).slice(0, 4).map((label) => ({ label }));\n      const more = isTool && (item.chips || []).length > 4;\n      return {\n        isNote: !isTool, isTool, text: item.text || '', title: item.title || '',\n        active: isTool && active, idle: isTool && !active,\n        done: isTool && !active, pending: isTool && active,\n        hasChips: isTool && chips.length > 0, chips, more,\n        moreLabel: more ? `+${item.chips.length - 4} more` : '',\n      };\n    });\n    const toolCount = rows.filter((r) => r.isTool).length;\n    const signOn = s.signOn;\n    const signIdle = s.signState === 'idle';\n    const signPressed = s.signState === 'pressed';\n    const signSigned = s.signState === 'signed';\n    const capIdx = s.answerOn || s.collapsed ? 3 : s.shown >= 8 ? 2 : s.shown >= 6 ? 2 : s.shown >= 2 ? 1 : 0;\n    return {\n      bubbleOn: s.bubbleOn,\n      typedText: this.MSG.slice(0, s.typed),\n      cursorOn: s.bubbleOn && !typingDone,\n      traceOpen: s.traceOn && !s.collapsed,\n      stepsOn: toolCount > 0,\n      stepsLabel: `${toolCount} ${toolCount === 1 ? 'step' : 'steps'}`,\n      hasRows: s.shown > 0,\n      rows,\n      collapsed: s.collapsed,\n      answerOn: s.answerOn,\n      expanded: s.expanded,\n      notExpanded: !s.expanded,\n      signOn, signIdle, signPressed, signSigned,\n      ...this.installVals(),\n      ...this.faqVals(),\n      ...this.terminalVals(),\n      ...this.surfaceVals(),\n      ...(() => {\n        const t = s.wtab, w = this.WALLETS[t], out = {\n          wtab0: () => this.setState({ wtab: 0 }), wtab1: () => this.setState({ wtab: 1 }), wtab2: () => this.setState({ wtab: 2 }),\n          wBrowser: t === 0, wEmbedded: t !== 0, wImportOn: w.importOn,\n          wProvider: w.provider, wEnv: w.env, wFootnote: w.foot,\n        };\n        [0, 1, 2].forEach((i) => {\n          out[`w${i}Border`] = i === t ? '#5288c2' : '#e4e4e7';\n          out[`w${i}Bg`] = i === t ? '#fff' : 'transparent';\n          out[`w${i}TabColor`] = i === t ? '#09090b' : '#71717a';\n          out[`w${i}TabWeight`] = i === t ? 600 : 500;\n          out[`w${i}TabBg`] = i === t ? '#09090b' : 'transparent';\n        });\n        return out;\n      })(),\n      pillHover: { background: '#f4f4f5', borderColor: '#d4d4d8' },\n      toggleExpanded: () => this.setState((st) => ({ expanded: !st.expanded }), () => {\n        if (!this.state.expanded && this.loopWaiting) { this.loopWaiting = false; this.resetRun(); this.at(400, () => this.start()); }\n      }),\n      allRows: this.ITEMS.map((item) => ({\n        isNote: item.kind === 'note', isTool: item.kind === 'tool',\n        text: item.text || '', title: item.title || '',\n        hasChips: item.kind === 'tool' && (item.chips || []).length > 0,\n        chips: (item.chips || []).map((label) => ({ label })),\n      })),\n      captions: this.CAPTIONS.map((c, i) => ({ ...c, on: i === capIdx, off: i !== capIdx })),\n    };\n  }\n}\n";
